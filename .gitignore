# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
.pytest_cache/
.ruff_cache/

# Secrets / local config — only .env.example (placeholders) is committed
.env
.env.*
!.env.example

# Data (raw pulls and derived artifacts stay out of git)
data/

# ML artifacts
mlruns/
models/
# Derived benchmark tables are regenerable from data/raw; the served model
# pickle is small and stays tracked so deploys have it.
artifacts/*.parquet
mlflow.db

# OS / editor
.DS_Store
.idea/
.vscode/
